from setuptools import setup, find_packages
setup(
  name = 'pantheon',
  packages = find_packages(), # this must be the same as the name above
  version = '1.0.0',
  description = 'Riot API library for Python and asyncio',
  author = 'Canisback',
  author_email = 'canisback@gmail.com',
  url = 'https://github.com/Canisback/pantheon', # use the URL to the github repo
  download_url = 'https://github.com/Canisback/pantheon/archive/0.1.tar.gz', # I'll explain this in a second
  keywords = ['Riot API', 'python'], # arbitrary keywords
  classifiers = [],
)
